const myStates = ['Rajastan', 'Kerala', 2008, 'Goa', 'Banglore']

// FOR EACH LOOP is not technically considered as loop in a array it is considered as method in array


myStates.forEach((s) => (console.log(s)));